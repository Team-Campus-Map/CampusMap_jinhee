import React from "react";

const TimetableDetail = ({detailheader}) => {
  return <div className="timetable-Detail-Container">
    <div className="timetable-Detail-Container-header">{detailheader}</div>
  </div>;
};

export default TimetableDetail;
