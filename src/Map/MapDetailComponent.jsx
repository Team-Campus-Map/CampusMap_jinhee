import React from "react";

const MapDetailComponent = ({ header, body }) => {
  return (
    <div>
      <div>{header}</div>
      <div>{body}</div>
    </div>
  );
};

export default MapDetailComponent;
